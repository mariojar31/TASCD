import { StyleSheet } from "react-native";

import { COLORS, FONT, SIZES } from "../../constants";

const styles = StyleSheet.create({

    iconVisible: {
        padding: "5vw", color:COLORS.secondarylight
    },
    iconHide:{
        display:'none'
    },

})

export default styles;

